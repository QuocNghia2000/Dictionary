<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginTop="5dp"
    android:paddingLeft="@dimen/page_padding"
    android:paddingRight="@dimen/page_padding">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@drawable/bg_item_home"
        android:gravity="center_vertical"
        android:orientation="horizontal"
        android:paddingStart="10dp"
        android:paddingTop="6dp"
        android:paddingEnd="0dp"
        android:paddingBottom="6dp">

        <LinearLayout
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginEnd="-15dp"
            android:layout_weight="1"
            android:orientation="vertical"
            android:paddingTop="5dp"
            android:paddingBottom="5dp">

            <TextView
                android:id="@+id/tvWord"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="Word"
                android:textColor="@color/bold_word"
                android:textSize="14sp" />

            <TextView
                android:id="@+id/tvMean"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="Mean"
                android:textColor="@color/mean"
                android:textSize="14sp" />
        </LinearLayout>

        <ImageView
            android:id="@+id/btnSound"
            android:layout_width="45dp"
            android:layout_height="45dp"
            android:contentDescription="Text sound"
            android:padding="8dp"
            android:scaleType="fitCenter"
            android:src="@drawable/ic_sound"
            android:visibility="visible" />

        <ImageView
            android:id="@+id/iconFavorite"
            android:layout_width="45dp"
            android:layout_height="45dp"
            android:layout_gravity="center_vertical"
            android:layout_marginBottom="2dp"
            android:contentDescription="Translate text favorite"
            android:padding="12dp"
            android:paddingTop="10dp"
            android:scaleType="fitCenter"
            android:src="@drawable/ic_star_off" />

        <ImageView
            android:id="@+id/iconDelete"
            android:layout_width="45dp"
            android:layout_height="45dp"
            android:layout_gravity="center_vertical"
            android:contentDescription="Translate text delete"
            android:padding="10dp"
            android:scaleType="fitCenter"
            android:src="@drawable/ic_delete"
            android:visibility="gone" />
    </LinearLayout>
</LinearLayout>
